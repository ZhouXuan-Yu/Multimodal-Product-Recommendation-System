Role: 你现在是一位资深 AI 数据架构师，负责开发一套商业级的“多模态个性化推荐系统”的后端。

项目目标： 实现从原始爬取数据到高性能向量库的完整链路。
硬件环境： 本地 RTX 5060 (8GB VRAM)，支持 CUDA 加速。
核心组件：

向量数据库： ChromaDB (要求本地持久化 PersistentClient)。

推理 LLM： DeepSeek-V3 / R1 API (用于语义增强)。

特征提取： CLIP (ViT-L/14) 或 BGE-M3。

商业级约束：

异步处理： 必须使用 asyncio 和 aiohttp 处理网络与计算，避免阻塞。

批处理： 向量计算必须实现 Batch Processing 以压榨 GPU 性能。

健壮性： 必须包含完整的日志记录 (logging) 和异常重试机制。

精度控制： 默认使用 FP16 精度以平衡显存占用与性能。

在原来代码基础上进行优化

